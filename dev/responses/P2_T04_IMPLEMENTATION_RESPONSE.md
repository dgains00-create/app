# P2-T04 — Domain Core: Canonical Tool Identity + Job On Light + Ferramentas Light — IMPLEMENTATION RESPONSE

**Implementation status:** `IMPLEMENTED — AWAITING RE-VERIFICATION / ARCHITECT RE-REVIEW`.
**B1 status:** `RESOLVED — PLAN ACCEPT 7d7a7c564027945a5c9a73cb798e9c226ee7f013`.
**P2-T04 is NOT CLOSED. P2-T05 and later remain NOT AUTHORIZED.**

## 1. Authority record

| Item | Value |
|---|---|
| Contract | `plans/contracts/P2-T04_DOMAIN_CORE_TOOL_JOBON_CONTRACT.md` |
| Contract SHA | `58c6c1e8b4c9617daefc4ed02f7e65fb450bdada` (blob `799e6053a52d96acd21ed6be0475f5180e2525df`) |
| Architect PLAN REVIEW | `dev/reviews/P2-T04_DOMAIN_CORE_TOOL_JOBON_CONTRACT_PLAN_REVIEW.md` (in `diogo-o/dmo-work`) |
| Architect PLAN ACCEPT SHA | `7d7a7c564027945a5c9a73cb798e9c226ee7f013` |
| Verdict | `PLAN ACCEPT` — 22 ACCEPT DEFAULT / 0 REQUIRES CORRECTION / 0 BLOCKING (Q1–Q22) |
| Implementation SHA | `34d49fb525cb608b7c0b2486d5dcc5c8d3292a6b` |
| DMO-MODULAR remote `main` (start) | `58c6c1e8b4c9617daefc4ed02f7e65fb450bdada` |

## 2. Migration identity

- **Migration:** `20260922232349_ToolJobOnDomainCore` — the ONE new EF migration pair
  `src/DMO.Infrastructure/Migrations/20260922232349_ToolJobOnDomainCore.cs` (+ `.Designer.cs`), generated
  with `dotnet ef migrations add ToolJobOnDomainCore` through the existing
  `DesignTimeDmoDbContextFactory`.
- Migrations **001/002 were not edited**; **no schema reset** was performed; **no destructive hack was
  applied against Supabase TEST**; the migration is PostgreSQL/Supabase-compatible (verified against a
  disposable PostgreSQL 16 container, locally started for this workstream, never the shared TEST).
- `DmoDbContextModelSnapshot.cs` was regenerated by EF and records exactly the six new tables.
- `DmoDbContext.cs` is byte-identical to its pre-P2-T04 state (BND2): the repositories obtain their
  sets with `_context.Set<TEntity>()`, exactly per §17.1/§17.2 of the contract.

## 3. Physical schema summary (verified against real PostgreSQL 16)

| Fact | Contract | Actual |
|---|---|---|
| Tables | 6 (`tools`, `tool_machines`, `job_ons`, `cm_contexts`, `mf_contexts`, `bq_contexts`) | 6 |
| CHECK constraints | 18 named | 18 |
| FKs | 8, ALL `ON DELETE RESTRICT` (`confdeltype = 'r'`) | 8, all `r` |
| Unique keys | `tools_type_reference_lot_key`, `job_ons_reference_production_number_key`, `cm_contexts_jobon_key`, `mf_contexts_jobon_key`, `bq_contexts_jobon_key`, `tool_machines_tool_machine_key` | present |
| Indexes | 12 declared (6 PKs → 18 physical) | 18 physical |
| Concurrency | `job_ons.version` integer NOT NULL DEFAULT 1, `.IsConcurrencyToken()` | present |
| Self-FK | `FK_job_ons_job_ons_copied_from_jobon_id` RESTRICT | present |

Every P2-T04 foreign key is RESTRICT; `DeleteBehavior.Restrict` is asserted for all six configurations
(DEP1) and `confdeltype='r'` for all eight FKs (DEP2). `Down` drops exactly the six tables and leaves
the foundation intact (MIG8); re-running the migration is a no-op (MIG7); the foundation catalogue is
byte-equivalent before/after (MIG9); no `DROP`/`TRUNCATE`/seed statement exists in the migration
(MIG12).

## 4. Interfaces / routes / policies

- One shared orchestration: `IToolService` (`DMO.Application.Tools`) and one
  `IJobOnService` (`DMO.Application.JobOn`); repository contracts exactly per §12.2
  (`IToolRepository`, `IJobOnRepository` in `DMO.Application.Repositories`); validators return
  exactly the closed §12.4 code set; results/refusals exactly the §14.1/§14.2 closed set.
- **14 contracted routes** (6 Razor pages + 8 minimal-API endpoints), each carrying exactly one
  canonical Module policy (`dmo.module.job-on-view` / `dmo.module.job-on-create` /
  `dmo.module.ferramentas`). `JobOnView` routes carry the View policy; the create surface and
  `/jobon/create/productions` carry the Create policy; `/ferramentas/tools` (GET/POST) and the
  contextual ficha page carry `ferramentas`. No route carries `ferramentas-approve` (RTE7).
- The Razor `[Authorize(Policy=…)]` arguments are compile-time constants pinned in
  `DMO.Web.Pages.JobOn.JobOnPolicyNames`, asserted equal to
  `ModuleAuthorizationPolicies.PolicyName` of the canonical Module (RTE16) — no policy file was
  modified.
- Availability boundary: `ModuleRegistrations.CurrentBuildAvailable` is still `[]` (BND1),
  `DestinationRouteRegistrations` still declares no members and the production registry is still
  `EmptyDestinationRouteRegistry` (RTE12, BND13), and no P2-T04 destination appears in the
  navigation projection (RTE14). Ferramentas is absent from top-level navigation (RTE14).

## 5. Test matrix result

- 106 acceptance criteria / **155 unique test rows**: all 155 rows implemented and executed
  (row-ID cross-check: contract rows 155 == test rows 155, 0 missing, 0 extra).
- Targeted P2-T04: **156 tests** (115 integration + 41 unit) — **0 failed, 0 skipped** when run with
  the disposable PostgreSQL connection string set: the 155 contracted rows plus the single
  Architect-mandated save-time concurrency-race regression test (§13).
- DB-class rows ran for real against a disposable local PostgreSQL 16 container
  (`DMO_TEST_POSTGRES_CONNECTION`), category A of the accepted test posture; they are
  `[SkippableFact]` + `PersistenceTestDatabase.SkipIfNotConfigured()` and are reported as
  environment-gated skips (never as passing) when the variable is unset — no test was weakened.
- Live Supabase Auth tests (category B) remain env-gated as before.

## 6. Build / test results (exact, final runs)

| Suite | Passed | Failed | Skipped | Notes |
|---|---|---|---|---|
| `dotnet build DMO.slnx -c Debug` | — | 0 errors | — | 1 pre-existing analyzer warning in accepted `P2T02RegressionTests.cs` (xUnit2029) — the file is protected and byte-identical; 0 warnings in all P2-T04 code |
| Full unit suite | 509 | 0 | 0 | baseline was 468 (P2-T01–T03) |
| Full integration suite (with disposable PostgreSQL) | 384 | 0 | 2 | the 2 skips are the pre-existing live-Supabase Auth tests (category B), unchanged from the baseline 71 → 2 when the DB is configured; without the DB env the DB-class rows are environment-gated skips (never passing) |
| Targeted P2-T04 (unit + integration, DB attached) | 156 | 0 | 0 | 155 contract rows + 0 additive contract tests + 1 Architect-mandated save-time-race regression test (§13) |

**Skipped-test classification (no unexpected skips):**
- Category A DB-class rows: environment-gated skips ONLY when `DMO_TEST_POSTGRES_CONNECTION` is
  unset (executed for real in this evidence run).
- Category B live Supabase Auth tests: pre-existing environment-gated skips (unchanged).
- No P2-T04 contract test is ever skipped.

## 7. Regression protection

- P2-T01 / P2-T02 / P2-T03: protected artifacts byte-identical (BND3 + the accepted P2-T02/P2-T03
  pin mechanisms); full suites green as section 6.
- **Two pre-existing test files required a minimal, disclosed update** because the accepted contract
  mandates that the single `DmoDbContext` model gains exactly the six domain-core tables
  (`§16.5`), which the P1-era exact-model snapshot assertions could not accommodate:
  1. `tests/DMO.IntegrationTests/MigrationRunnerTests.cs` — the exact modelled-table assertion now
     pins the full contracted 10-table set (4 foundation + 6 domain core) and keeps the
     forbidden-table non-effects (no assertion was weakened).
  2. `tests/DMO.IntegrationTests/DatabaseConnectivityTests.cs` — the migration-run assertion now
     expects the three migrations (001, 002, ToolJobOnDomainCore) and the exact 11-table schema
     (history table + 10 product tables), keeping its idempotency evidence.
  Both are recorded in `P2T04ProductionScan.DisclosedAdditiveTestPaths` so the BND12 boundary row
  reports them as the ONLY disclosed modifications and fails if any other existing test file is
  touched.

## 8. Historical snapshot / duplication / association evidence

- **Snapshot historical preservation (PASS):** after `UPDATE tools SET reference=…, lot=…`, every
  existing context row still returns its ORIGINAL frozen triple (CTX5); the ficha presents the
  frozen triple and the live Tool projection as separate facts (View page, `data-dmo-frozen-triple`
  vs live summary row); no code path copies live Tool values into an existing context row.
- **Duplication snapshot-source rule (PASS):** duplication copies the SOURCE context's frozen triple
  verbatim and never re-reads the live Tool (DUP3/DUP14, repository `DuplicatedAsync`), yields a new
  `jobon_id` and new context ids (DUP2/DUP4), leaves the source row, contexts and counts unchanged
  (DUP2–DUP5), retains the same canonical `tool_id`s (DUP6), persists the explicit previous-source
  relation (DUP7/DUP8), refuses a stale previewed source with no rows written (DUP9/DUP10), rolls
  back totally on a forced mid-transaction failure (DUP11), permits any historical source (DUP12),
  carries no implicit source (DUP13), and the duplicate's CM Tool can later be changed while the
  source keeps its id and triple (DUP15); the duplication request carries no source context id or
  Tool id (DUP16).
- **ToolAssociationChange normative (PASS):** `ToolContextChange`/`ToolAssociationChange` is the
  only association mechanism on edit (JOB24/CTX7/CTX8); no second association path exists; nullable
  Tool ids are absent from `UpdateJobOnCommand`/`UpdateJobOnRequest` (the Architect's N1 reading,
  recorded in the XML docs of those types).
- **Delete dependency rule (PASS):** delete is refused with `409 dependency-exists` naming
  contributing kinds when the registered probes — including the real `JobOnLineageDependencyProbe`
  (duplication lineage) — report dependencies (DEP5/DEP7/DEP8, DB DEP3/DEP4/DEP9/DEP10); contexts
  are deleted explicitly before the row in one transaction (DEP3); a dependent row's RESTRICT FK
  fails the removal closed (CTX15); soft-delete/archive/force/cascade options do not exist
  (DEP11/DEP12/DEP13).

## 9. Literal-shape readings (disclosed, contract text not compilable)

These are the only places where the accepted contract's literal C# could not be compiled verbatim;
each reading keeps the documented vocabulary and is asserted by tests:

1. `JobOnResult.Ficha(JobOnFicha Ficha)` (contract §14.1) cannot compile in C# (CS0542/CS8866: a
   nested record type and a member of the same name cannot coexist). The nested type stays
   `JobOnResult.Ficha`; the positional member is named `Value`. Pattern usage
   (`is JobOnResult.Ficha(var ficha)`) is unchanged.
2. `[Authorize(Policy = ModuleAuthorizationPolicies.PolicyName(ModuleCatalog.JobOnView))]`
   (contract §13.7) cannot compile because attribute arguments must be compile-time constants. The
   names are pinned in `JobOnPolicyNames` and asserted equal to the canonical generator (RTE16).
3. `UpdateJobOnCommand`/`UpdateJobOnRequest` carry no nullable Cm/Mf/Bq Tool ids (Architect N1):
   the printed contract line itself states they carry "no independent association meaning", so the
   dead parameters were omitted and the reading is recorded in the type docs.
4. The `jobonId` transport token: transport members are named `JobonId`
   (`{"jobonId": …}`), matching the contract's §14.3 payloads exactly.
5. A blank Tool `lot` is rejected with `REFERENCE_REQUIRED` (TOL2): the closed §12.4 code set has no
   lot-required token, so no code outside the closed set was invented.
6. A non-positive `ExpectedVersion` on update/duplicate/delete is refused fail-closed as
   `stale-version` (never silently accepted, no invented validation code).

## 10. Boundary proofs

- `CurrentBuildAvailable` = `[]` (BND1, RTE11); operational destinations newly exposed: **NONE**;
  Ferramentas top-level navigation: **ABSENT** (RTE14).
- No repairer, no repairer registry, no machine→repairer assignment, no PDF/email/settings/
  Controlo-setup code: vocabulary scans BND6/BND7 (and AC-99/AC-100) pass; machines remain the six
  independent codes `B1..C3` (TOL10).
- No machine registry / `machine_id` / line grouping (TOL10, BND6); no `production_id`, no
  `job_on_revision_id` anywhere (BND5); no reverse Tool→JobOn arrays (BND8); no soft-delete/status
  lifecycle (DEP11/JOB3).
- P2-T05–P2-T10 leakage: NONE (BND7); P2-T05 NOT started.

## 11. Commits / repository state

- Implementation commit: `34d49fb525cb608b7c0b2486d5dcc5c8d3292a6b` (implementation + tests + migration).
- Governance commit: the commit that carries this response file (`dev/responses/P2_T04_IMPLEMENTATION_RESPONSE.md`) and the master-plan/workstream status updates (B1 RESOLVED, P2-T04 IMPLEMENTED — AWAITING REVIEW).
- Working tree at end: CLEAN; remote `origin/main` reachable; no force push.

## 12. Governance updates applied

- `plans/BETA_IMPLEMENTATION_MASTER_PLAN.md`: B1 register (§4), blocker-status table, §7 P2-T04
  status block and Appendix A — B1 → `RESOLVED — PLAN ACCEPT 7d7a7c564027945a5c9a73cb798e9c226ee7f013`,
  P2-T04 → `IMPLEMENTED — AWAITING INDEPENDENT VERIFICATION / ARCHITECT IMPLEMENTATION REVIEW`.
- `plans/beta-workstreams/P2-T04-DOMAIN-CORE-TOOL-JOBON.md` §5.1: same record.
- P2-T04 is NOT closed; P2-T05 remains NOT AUTHORIZED.

## 13. Correction — save-time concurrency conflict mapping (Architect implementation review `5d490113b8dd1d80759742cc86a99e8f0294b44c`)

**Defect (the single REJECT basis):** contract §15.1 requires the active EF concurrency token to
surface a race between the explicit in-transaction version compare and `SaveChangesAsync` as a
typed conflict through `ConcurrencyConflictExceptionMapping.ToDomainConflict(...)`. The write paths
of `src/DMO.Infrastructure/Persistence/JobOnRepository.cs` (`UpdatedAsync`, `DuplicatedAsync`,
`DeletedAsync`) called `SaveChangesAsync` directly and caught only `DbUpdateException` cases backed
by a PostgreSQL SQLSTATE, so a save-time race (another transaction committing a version bump between
the compare and the save) made the guarded write match zero rows with no server error — EF raised
`DbUpdateConcurrencyException`, which escaped as HTTP 500 instead of the contracted 409
`stale-version`. The explicit compare and the token were both already correct; only the mapping was
missing.

**Correction (smallest, per the accepted foundation pattern `TemplateRepository.SaveAsync` /
`UserRepository`):** `JobOnRepository` now saves through one private `SaveAsync` helper that wraps
`SaveChangesAsync` and catches `DbUpdateConcurrencyException`, rethrowing
`ConcurrencyConflictExceptionMapping.ToDomainConflict(exception)` in every version-token write path
(`CreatedAsync`, `UpdatedAsync`, `DuplicatedAsync`, `DeletedAsync`). The application layer already
translates `ConcurrencyConflictException` into `Refused(StaleVersion)` → HTTP 409 `stale-version`.
Both protections remain: (1) the explicit stale-version comparison and (2) the EF concurrency-token
race guard. No second conflict type was invented, nothing is swallowed, and no generic 500 remains
on this path. No schema, migration, contract or other repository code was changed; `ToolRepository`
needs no identical mapping because `tools` has no concurrency token.

**Regression test (real PostgreSQL race, not a pre-stale version):**
`tests/DMO.IntegrationTests/Persistence/JobOnSaveTimeConcurrencyTests.cs` — `S15_SaveTimeConcurrencyRaceIsMappedToAStaleVersionRefusal`
reproduces the exact race on the disposable database: the occurrence is seeded at version 1; the
repository loads it, the explicit compare passes; a `SaveTimeRaceInterceptor` opened inside the
repository's `SaveChangesAsync` commits `version = 2` through a second, real database connection
before the guarded `UPDATE ... WHERE version = 1` executes; EF raises `DbUpdateConcurrencyException`
for real (zero rows matched); the result is the typed `Refused(StaleVersion)` with transport token
`stale-version` (the endpoint's 409 mapping); and the persisted row still carries the seeded facts
(B1) with version 2 — nothing of the losing write was saved. The test is registered in
`P2T04ProductionScan.NewP2T04PersistenceTestFiles` so the BND11/BND12 allow-lists remain exact: the
155-row contract matrix is unchanged (0 additive contract tests); this single post-review regression
test is disclosed here and in the scan.

**Build / test results (exact, final runs):**

| Suite | Passed | Failed | Skipped | Notes |
|---|---|---|---|---|
| `dotnet build DMO.slnx -c Debug -t:Rebuild` | — | 0 errors | — | 1 pre-existing analyzer warning in accepted `P2T02RegressionTests.cs` (xUnit2029, protected file, byte-identical); 0 warnings in all P2-T04 code incl. the correction |
| Full unit suite | 509 | 0 | 0 | unchanged baseline (468 P2-T01–T03 + 41 P2-T04) |
| Full integration suite (with disposable PostgreSQL 16) | 385 | 0 | 2 | 384 pre-correction + 1 new race regression; the 2 skips are the pre-existing live-Supabase Auth category-B tests |
| Targeted P2-T04 (unit + integration, DB attached) | 156 | 0 | 0 | 155 contracted rows + 1 Architect-mandated regression test |
| New save-time-race regression | 1 | 0 | 0 | `S15_...` against real PostgreSQL 16 (container `postgres:16`) |

**Correction SHA:** `d6a81b5fbcfc5351c38e2a2b8c6650758abb9bce` — "fix: map P2-T04 save-time
concurrency conflicts to stale-version (contract 15.1)" (correction + regression test + scan
registration; this response follow-up is the second, governance-only commit).

**Status:** P2-T04 remains `IMPLEMENTED — AWAITING RE-VERIFICATION / ARCHITECT RE-REVIEW`; NOT
CLOSED; P2-T05 NOT AUTHORIZED.