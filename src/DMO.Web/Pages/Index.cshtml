@page
@model IndexModel
@{
    ViewData["Title"] = "Área operacional";
}
@* P1-T07: "/" is an account-aware routing surface — every GET path redirects (/Login,
   /Administration, a real registered landing route, or /AccessDenied). This page never
   renders: the A2 placeholder content was superseded by the accepted P1-T07 root contract
   (request §13); no shell UI lives here. *@