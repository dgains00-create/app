# Module Registry

Registry of modules available to the runtime. The registry describes discoverability, navigation and access requirements; it must not contain the business implementation of those modules.
